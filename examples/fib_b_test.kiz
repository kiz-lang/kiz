start = now()
fn fib(n)
    a=0
    b=1
    i=0
    while i<n
        c=a+b
        a=b
        b=c
        i=i+1
    end
    return a
end

print("fib(30) =", fib(30))
print("using", now()-start, "ns")